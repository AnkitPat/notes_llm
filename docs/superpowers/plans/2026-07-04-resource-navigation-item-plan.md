# ResourceNavigationItem Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement `ResourceNavigationItem` and refactor `ResourceNavigation` to use it.

**Architecture:** Create a new component `ResourceNavigationItem.tsx` and map resource types to icons. Update `ResourceNavigation.tsx` to iterate over the new component.

**Tech Stack:** React, Next.js, MUI.

---

### Task 1: Create ResourceNavigationItem Component

**Files:**
- Create: `frontend/src/components/ResourceNavigationItem.tsx`
- Test: `frontend/src/__tests__/ResourceNavigationItem.test.tsx` (Need to create this)

- [ ] **Step 1: Write tests for ResourceNavigationItem**
- [ ] **Step 2: Implement ResourceNavigationItem**
- [ ] **Step 3: Verify tests pass**
- [ ] **Step 4: Commit**

### Task 2: Refactor ResourceNavigation

**Files:**
- Modify: `frontend/src/components/ResourceNavigation.tsx`
- Test: `frontend/src/__tests__/ResourceNavigation.test.tsx` (Update if needed)

- [ ] **Step 1: Update existing tests if needed**
- [ ] **Step 2: Modify ResourceNavigation.tsx**
- [ ] **Step 3: Verify tests pass**
- [ ] **Step 4: Commit**
